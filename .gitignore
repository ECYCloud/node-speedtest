# Build artifacts
*.exe
*.o
*.obj
*.dll
*.so
*.dylib
*.a
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
Makefile
build/
out/

# Bundled mihomo kernel (downloaded separately, too large for git)
base/tools/clients/

# Test fixtures with real subscription / node credentials (DO NOT COMMIT)
sub_test.yaml
test/expected-*-node.yaml

# Speedtest runtime output
config.yaml
config.json
results/

# 例外:engine-assets 下的 config.yaml 是 mihomo 配置模板,必须进仓库供 CI 使用
!separated/desktop/engine-assets/config.yaml

# CI artifact downloads and locally assembled release bundles
artifacts/
stairspeedtest-mihomo-*/

# Local build output / staging directories (Vite / Tauri / CI assemble step)
dist/
separated/desktop/dist/
separated/desktop/src-tauri/target/
separated/desktop/src-tauri/engine/
**/tauri_build.log

# Temporary mihomo process output files
mihomo.out
mihomo.err
mihomo.pid

# Local debug / log dumps
linux64_log.txt

# Editor / OS junk
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Local scratch (don't commit)
.tmp_old_render.txt
*.tmp.txt

